package bank;

public class Bank {
    //Bank должен реализовывать 1 интерфейс, обязывающий проверять курс валют,
    // который изменяется при каждом запросе на значение от 0 до 10(тип double)
    // и также хранить баланс своих клиентов, а также локальный класс, создающий счёт.
}
